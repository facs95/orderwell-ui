import React from "react";
import { Link } from "react-router-dom";

export const Orders = () => {
    return (
        <div>
            Orders
            <p></p>
            <Link to="/">Dashboard</Link>
        </div>
    );
};
